//import { action } from 'typesafe-actions';
import { ActionTypes } from './ActionTypes';
import { IItem } from '../models/IItem';

export const itemsActions = {
  // addItemRequest: (item: IItem) => action(ActionTypes.ADD_ITEM_REQUEST, item),
  // addItemSuccess: (item: IItem) => action(ActionTypes.ADD_ITEM_SUCCESS, item),
  // addItemError: (error: Error) => action(ActionTypes.ADD_ITEM_ERROR, error.message),

  // getItemsRequest: () => action(ActionTypes.GET_ITEMS_REQUEST),
  // getItemsSuccess: (items: IItem[]) => action(ActionTypes.GET_ITEMS_SUCCESS, items),
  // getItemsError: (error: Error) => action(ActionTypes.GET_ITEMS_ERROR, error.message)
};
